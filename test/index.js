var assert = require('assert');

describe("Test notes endpoint", function() {

    //------------------------------------------------------------------------    
    it("should always pass", function(done) {
        assert(true);
        done();
    });
});